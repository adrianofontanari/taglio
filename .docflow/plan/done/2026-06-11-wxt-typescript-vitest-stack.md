# WXT + TypeScript + Vitest stack

Owning ADR: `adr/0004-wxt-typescript-vitest-stack.md`

Adopted WXT for MV3 scaffolding/dev/build, TypeScript for the compile
gate, and Vitest as the test runner — all dev-only dependencies.

---
Shipped at HEAD `86c4f28` (backfilled 2026-07-23 — recorded after the fact).
