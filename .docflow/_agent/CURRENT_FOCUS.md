# Current Focus

This file is the LIVE SNAPSHOT of any in-flight session. It is short on
purpose — the durable record lives in git (`git log`),
`_agent/WORKLOG.md`, and `plan/done/`. The queued work lives in
`plan/todo/`.

If status files and git disagree, git is authoritative; correct this
file.

## Active state

- **Branch:** main
- **Active item:** none — no work in flight. Project is paused (see
  `AGENTS.md` §Project rules — Phase 0 shipped, Phase 1 frozen
  since 2026-07-15).
- **Blockers:** none.
- **Uncommitted work:** none (docflow bootstrap files only).

## Last shipped

None yet via the `plan/` queue — docflow bootstrap just landed
(`.docflow/adr/0001`).

## Next item

`ls plan/todo/` for the queue; the lowest-numbered file runs next.
Queue is currently empty.
