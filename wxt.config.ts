import { defineConfig } from 'wxt';

export default defineConfig({
  manifest: {
    name: 'Taglio',
    description: 'Cuts PII out of your prompts before they reach the LLM.',
    version: '0.1.0',
  },
});
